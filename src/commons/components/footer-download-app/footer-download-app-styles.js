import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles((theme) => ({
  title: {
    textAlign: 'center',
  },
  androidIcon: {
    fontSize: "100px",
    color: '#689f38',
  },
  appleIcon: {
    fontSize: "110px",
    color: '#e0e0e0',
  },
}))

export default useStyles;