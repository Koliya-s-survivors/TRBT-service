import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles(() => ({
  title: {
    textAlign: 'center',
  },
  androidIcon: {
    fontSize: "110px",
    color: '#689f38',
  },
  appleIcon: {
    fontSize: "120px",
    color: '#e0e0e0',    
  },
}))

export default useStyles;