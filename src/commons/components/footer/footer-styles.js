import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles((theme) => ({
    container: {
        background: "darkgrey",
        padding: "24px"
    },
    info: {
        marginTop: "10px"
    }
}));

export default useStyles;